<project name="HyProxy" basedir="." default="main">

    <property name="src.dir"     value="src"/>

    <property name="build.dir"   value="build"/>
    <property name="classes.dir" value="classes"/>
    <property name="jar.dir"     value="jar"/>
    <property name="tomcatlib.dir"     value="/usr/share/maven-repo/org/apache/tomcat/tomcat-servlet-api/3.0"/>
    <property name="httpclient.dir"    value="/usr/share/maven-repo/org/apache/httpcomponents-client/httpclient/"/>
    <property name="httpcore.dir"      value="/usr/share/maven-repo/org/apache/httpcomponents-client/httpcore/"/>
    <property name="commons.dir"       value="/usr/share/maven-repo/org/apache/commons/"/>
    <property name="slf4j.dir"         value="/usr/share/maven-repo/org/apache/slf4j/"/>
    
    
    <path id="classpath">
        <fileset dir="${httpclient.dir}" includes="**/*.jar"/>
        <fileset dir="${httpcore.dir}" includes="**/*.jar"/>
        <fileset dir="${commons.dir}" includes="**/*.jar"/>
        <fileset dir="${slf4j.dir}" includes="**/*.jar"/>
    </path>

    <property name="main-class"  value="Server.VPNServer"/>



    <target name="clean">
        <delete dir="${classes.dir}"/>
        <delete dir="${jar.dir}"/>
    </target>

    <target name="compile">
        <mkdir dir="${classes.dir}"/>
        <javac includeantruntime="false" srcdir="${src.dir}" destdir="${classes.dir}" classpathref="classpath"/>
    </target>

    <target name="jar" depends="compile">
        <mkdir dir="${jar.dir}"/>
        <jar destfile="${jar.dir}/${ant.project.name}.jar" basedir="${classes.dir}">
            <manifest>
                <attribute name="Main-Class" value="${main-class}"/>
            </manifest>
        </jar>
    </target>

    <target name="run" depends="jar">
        <java fork="true" classname="${main-class}">
            <classpath>
                <path refid="classpath"/>
                <path location="${jar.dir}/${ant.project.name}.jar"/>
            </classpath>
            <arg value="${arg1}"/>
        </java>
    </target>

    <target name="clean-build" depends="clean,jar"/>

    <target name="main" depends="compile"/>

</project>
